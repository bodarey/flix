<h3>
	<%= @genre.name %>
</h3>

<%= link_to 'Edit',edit_genre_path(@genre), class: 'btn btn-success' %>
<%= link_to 'Delete',@genre, method: 'delete',class: 'btn btn-success', data: {confirm: 'do you want to delete?'} %>
<br/><br/><br/>
 <h3>Movies on this genre:</h3>
<p>
<% @genre_movies.each do |movie| %>
  <%= link_to movie.title, movie %> <br/>
<% end %>

</p>