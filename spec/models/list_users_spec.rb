require 'rails_helper'

describe "Viewing the list of users" do

          before do 
              # user1 = User.create!(user_attributes(name: "Larryf", email: "larry@example.com",username: 'a1'))
            # user2 = User.create!(user_attributes(name: "Moef",   email: "moe@example.com",username: 'a2'))
           #  user3 = User.create!(user_attributes(name: "Curlyf", email: "curly@example.com",username: 'a3'))

  
                  end


  it "shows the users" do


    # visit users_url


    # expect(page).to have_link(user1.name)
    # expect(page).to have_link(user2.name)
    # expect(page).to have_link(user3.name)
                       end
                       it "shows the user's details" do
  # user = User.create!(user_attributes)

  

  # visit user_url(user)

  # expect(page).to have_text(user.name)
  # expect(page).to have_text(user.email)
end

end